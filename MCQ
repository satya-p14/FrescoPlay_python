1. The output of expression, k = [print(i) for i in "maverick" if i not in "aeiou"] is _. :  Prints all characters that are not vowels
2. What is the return type of function 'id'?  : int
3. The elements of an iterator can be accessed multiple times. : False
4. A generator function can have multiple yield expressions. : True
5. The output of the expression {0 if i%2 ==0 else 1 for i in range(8)} is . : {0, 1}
6. What value does 'k' hold after executing the expression, k = [print(i) for i in "maverick" if i not in "aeiou"]? : A list of None's
7. The output of the expression itertools.takewhile(lambda x: x<5, [1,4,6,4,1]) is _. : [1,4]
8. Which of the following types of arguments can be passed to a function? All the options
9. Which of the following are present in a function header? : Function name and parameter list
10. What is the output of the following code?

class A:
    def __init__(self, a = 5):
        self.a = a

        def f1(self):
            self.a += 10


class B(A):
    def __init__(self, b = 0):
        A.__init__(self, 4)
        self.b = b

    def f1(self):
        self.b += 10

x = B()
x.f1()
print(x.a,'-', x.b)

Ans  :  4 - 10

11.  What is the output of the following code?

class class1:
    a = 1

    def f1(self):
        a = 2
        class1.a += 1
        print(class1.a, end=' ')
        print(a, end=' ')

class1().f1()
class1().f1()

Ans : 2 2 3 2

12 . Which of the following method is used by a user-defined class to support '+' operator? : __add__
13. What is the output of the following code?

class A:
    def __init__(self):
        print('one')

    def f(self):
        print(float())
        print(hex(-255))

class B(A):
    def __init__(self):
        print('two')

    def f(self):
        print(float())
        print(hex(-42))

x = B()
x.f()

Ans :  two
       0.0
       -0x2a

14.  What is the output of the following code?

class A:
    x = 0

    def __init__(self, a, b):
        self.a = a
        self.b = b
        A.x += 1

    def __init__(self):
        A.x += 1

    def displayCount(self):
        print('Count : %d' % A.x)

    def display(self):
        print('a :', self.a, ' b :', self.b)

a1 = A('George', 25000)
a2 = A('John', 30000)
a3 = A()
a1.display()
a2.display()
print(A.x)

Ans : ERROR

15 : Which of the following exception occurs, when an integer object is added to a string object? : TypeError
16 : Can one block of except statements handle multiple exception? : Yes, like except ( NameError, SyntaxError, ...)
17.  How many except statements can a try-except block have? : more than zero
18. In which of the following scenarios, finally block is executed? : Always
19. Which of the keyword is used to display a customised error message to the user? : raise
20. Which of the following modules are used to deal with Data compression and archiving? : 
21. Which of the following methods of 'random' module is used to pick a single element, randomly, from a given list of elements? : choice
22. Which of the following module is not used for parsing command line arguments automatically? : cmdparse
23. Which of the following statement retreives names of all builtin objects? : import builtins; builtins.dict.keys()
24. Which of the following expression can be used to check if the file 'C:\Sample.txt' exists and is also a regular file? : os.path.isfile(C:\Sample.txt)
25.  Which of the following modules support regular expressions in Python? : re
26. What does the search method of re module do? :  It matches the pattern at any position of the string.
27. What is the output of the expression re.split(r'[aeiou]', 'abcdefghij')? : ['', 'bcd', 'fgh', 'j']
28. Which of the following expression is used to compile the pattern p : re.compile(p)
29. What does seek method of a file object do? : Moves the current file position to a different location at a defined offset.
30. Which of the following package provides the utilities to work with postgreSQL database? : psycopg2
31. Which of the following method is used to fetch next one row of a query result? : fetchone
32. Which of the following package provides the utilities to work with MySQLDB database? : MySQLdb
33. Which of the following method is used to insert multiple rows at a time in sqlite3 datatbase? : executemany
34. Which of the following package provides the utilities to work with MongoDB database? : pymongo
35. What is the output of the following code? : 24
    def f(x):
        return 3*x
    
    def g(x):
        return 4*x
    
    print(f(g(2)))

36. What is the output of the following code? :  [6, 6, 6, 6]

    def multipliers():
        return [lambda x : i * x for i in range(4)]
    
    print([m(2) for m in multipliers()])


37. What is the output of the following code? :

    def outer(x, y):
    
        def inner1():
            return x+y
    
        def inner2():
            return x*y
    
        return (inner1, inner2)
    
    
    (f1, f2) = outer(10, 25)
    
    print(f1())
    print(f2())

Answer :  35 
          250

38. A closure does not hold any data with it. : False
39. What is the output of the following code? : 

    def bind(func):
        func.data = 9
        return func
    
    @bind
    def add(x, y):
        return x + y
    
    print(add(3, 10))
    print(add.data)

Answer : 13
         9

40. Classes can also be decorated, if required, in Python. :  Yes
41. If a property named temp is defined in a class, which of the following decorator statement is required for deleting the temp attribute ? : @temp.deleter
42. Which of the following decorator function is used to create a class method? : classmethod
43. Which of the following decorator function is used to create a static method? : staticmethod
44. Which of the following module helps in creating abstract classes in Python? : abc
45. Which of the following methods have to be defined in a class to make it act like a context manager? : __enter__, __exit__
46. What does the context manager do when you are opening a file using with? : It closes the opened file automatically
47. A Coroutine is a generator object. : true























